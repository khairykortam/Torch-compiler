include "std.torch"

memory n sizeof(u64) end

const n 64 end

true while 
n @64 print
!32

n @64 1 = if
// we need to implement breaking mechanisms.
end
n @64 1 and 0 = if
n @64 2 / n !64
else
n @64 3 * 1 + n !64
end 
end do

"\n" eputs
